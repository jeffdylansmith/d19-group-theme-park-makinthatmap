{{#each areas}}

 <section id="card--{{@index}}" class="card-wrapper" style="border: 2px solid">
     <h3>#{{increment @index}} Cake </h3>
     <h4>{{name}} {{description}}</h4>

     <p class="card-copy"> {{description}}</p>

 </section>

{{/each}}