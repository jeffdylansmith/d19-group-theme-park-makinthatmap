{{#each this}}

	<div id="card{{@index}}" class="card-wrapper col-4 l-m-1" style="border: 2px solid">

	    <h3>#{{increment @index}} Theme Park </h3>
	    <h4>{{name}}</h4>
	    <br><p>{{description}}</p>

	</div>

{{/each}}